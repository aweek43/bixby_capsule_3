action (InputEnlist) {
  description (입대일 입력)
  collect {
    input (start) {
      type (start)
      min (Required) max (One)
    }
    input (where) {
      type (where)
      min (Optional) max (One)
    }
    
  }
  output (dummy)
  type (Search)
}